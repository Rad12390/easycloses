{% form_theme form 'bootstrap_3_layout.html.twig' %}

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true"></button>
    <h4 class="modal-title">User Business Settings</h4>
</div>
<div class="modal-body">
    <div class="row">
        <div class="col-md-12">
            {{ form_start(form) }}
                {{ form_row(form.quickBookId) }}
                {{ form_row(form.edit) }}
            {{ form_end(form) }}
        </div>
    </div>
</div>
